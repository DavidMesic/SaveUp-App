<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:SaveUpApp"
       x:Class="SaveUpApp.AppShell">
    <ShellContent Title="Home"
                  Route="MainPage"
                  ContentTemplate="{DataTemplate local:MainPage}" />
    <ShellContent Title="Dashboard"
                  Route="DashboardPage"
                  ContentTemplate="{DataTemplate local:DashboardPage}" />
    <ShellContent Title="Produkt erfassen"
                  Route="AddProductPage"
                  ContentTemplate="{DataTemplate local:AddProductPage}" />
    <ShellContent Title="Produktliste"
                  Route="ProductListPage"
                  ContentTemplate="{DataTemplate local:ProductListPage}" />
    <ShellContent Title="Über Uns"
                  Route="AboutUsPage"
                  ContentTemplate="{DataTemplate local:AboutUsPage}" />
</Shell>